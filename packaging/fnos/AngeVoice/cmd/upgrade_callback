#!/bin/bash
# 升级后处理：保留 TRIM_PKGVAR 下的模型、输出和 API Key；新版 compose 直接使用升级向导透传值。
set -eu
mkdir -p "${TRIM_PKGVAR}/models" "${TRIM_PKGVAR}/outputs" "${TRIM_PKGVAR}/prompts" "${TRIM_PKGVAR}/logs"
echo "[AngeVoice] 升级完成：模型、输出与 API Key 数据目录均已保留。"
exit 0
