# AngeVoice — pinned dependency constraints for source/CPU Docker builds.
# Only packages prone to uncontrolled upgrade are pinned here.
# torch/torchaudio are installed separately via ARGs with专用 index，不在此约束。
# Usage: pip install -c constraints.txt -e ".[zipvoice,tn]"

# ── core (drift-sensitive) ──────────────────────────
kokoro==0.8.2
misaki==0.8.2
jieba==0.42.1
pypinyin==0.50.0
transformers==4.46.3
tokenizers==0.20.3
huggingface_hub==0.26.5

# ── optional[moss] ─────────────────────────────────
sentencepiece==0.2.1
modelscope==1.27.0
onnxruntime==1.20.0

# ── optional[zipvoice] ─────────────────────────────
lhotse==1.24.0
vocos==0.1.0
pydub==0.25.1
cn2an==0.5.22
inflect==7.0.0
piper_phonemize==1.1.0
safetensors==0.4.5
setuptools<81

# ── optional[tn] ───────────────────────────────────
wetext==0.1.4
