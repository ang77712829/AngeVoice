# AngeVoice — GPU constraints lock (CUDA 12.x)
# For docker/gpu/Dockerfile builds.
# Only packages prone to uncontrolled upgrade are pinned here.
# torch/torchaudio are installed separately via ARGs from CUDA index.

# ── core (drift-sensitive) ──────────────────────────
kokoro==0.8.2
misaki==0.8.2
jieba==0.42.1
pypinyin==0.50.0
transformers==4.46.3
tokenizers==0.20.3
huggingface_hub==0.26.5
safetensors==0.4.5

# ── optional[moss] ─────────────────────────────────
sentencepiece==0.1.99
modelscope==1.20.0
onnxruntime-gpu==1.20.2
nvidia-cudnn-cu12==9.1.0.70

# ── optional[zipvoice] ─────────────────────────────
lhotse==1.24.0
vocos==0.1.0
pydub==0.25.1
cn2an==0.5.22
inflect==7.0.0
piper_phonemize==1.1.0
setuptools<81

# ── optional[tn] ───────────────────────────────────
wetext==0.1.4
